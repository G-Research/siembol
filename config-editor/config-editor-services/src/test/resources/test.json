{
  "field_mapping": [
    {
      "sigma_field": "sigma_user",
      "siembol_field": "siembol_user"
    }
  ],
  "rule_metadata_mapping": {
    "rule_name": "based_on_${title}_{id}",
    "rule_description": "generated from ${description}",
    "source_type": "secret_data",
    "tags": [
      {
        "tag_name": "sigma_tags",
        "tag_value": "${tags}"
      }
    ]
  }
}