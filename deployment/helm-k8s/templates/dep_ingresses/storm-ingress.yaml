{{- if (has "storm_ingress" .Values.enabled_apps) -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.storm_ingress.service.name }}
  namespace: {{ .Values.namespace }}
  annotations:
  {{- if .Values.certmanager.enabled }}
    cert-manager.io/issuer: ca-issuer
  {{- end }}
  {{- if .Values.storm_ingress.oauth2_proxy.enabled }}
    nginx.ingress.kubernetes.io/auth-signin: https://{{ .Values.storm_ingress.oauth2_proxy.host }}/oauth2/start
    nginx.ingress.kubernetes.io/auth-url: http://{{ .Values.storm_ingress.oauth2_proxy.host }}/oauth2/auth
    nginx.ingress.kubernetes.io/auth-response-headers: X-Auth-Request-User,X-Auth-Request-Email
  {{- end }}
spec:
  {{ if .Values.tls.enabled -}}
  tls:
  - hosts: 
    - {{ .Values.storm_ingress.fqdn }}
    secretName: {{ .Values.storm.appName }}-tls
  {{ end }}
  rules:
  - host: {{ .Values.storm_ingress.fqdn }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name:  {{ .Values.storm_ingress.service.name }}
            port: 
              number:  {{ .Values.storm_ingress.service.port }}
{{- end -}}