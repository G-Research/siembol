apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.serviceName }}
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080